Runtime-built SpeakFeedback wireframe.

L1_HelloFriends instantiates `SpeakFeedbackPanel` for Optional_Rec (`blockProgress=false`).
After opening the project in Unity Editor, run **Kids English → Create SpeakFeedback Prefab**
to write `SpeakFeedback.prefab` here (empty root + component; `Awake` builds children).
